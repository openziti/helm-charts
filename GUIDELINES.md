
- Batch related operations together
- Prefer exploration over asking — try an approach and report results
- When adding or removing a chart in `charts/`, update `.github/labeler.yml` to keep the `notes:<chart-name>` labels in sync with the list of charts
