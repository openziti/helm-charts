{{ if .Release.IsUpgrade }}
Your release {{ .Release.Name }} was upgraded.
{{ else if .Release.IsInstall }}
This is the first install of release {{ .Release.Name }}.
{{ else }}
{{ .Chart.Name }} was neither installed nor upgraded.
{{ end }}

{{- $mode := include "ziti-controller.clusterMode" . -}}
{{- if or (eq $mode "cluster-init") (eq $mode "cluster-join") -}}
  {{- $svcEnabled := (toString .Values.ctrlPlane.service.enabled | lower) -}}
  {{- if ne $svcEnabled "true" -}}
WARNING: This Ziti controller node's ctrl plane service is not published to other pods (ctrlPlane.service.enabled is not true), so it will not be possible to perform operations like adding another Ziti controller node via this node.
  {{- end -}}
{{- end -}}

You have chart version {{ .Chart.Version }} and app version {{ .Chart.AppVersion }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

This deployment provides an OpenZiti controller to manage an OpenZiti network.

Visit the console in a web browser: {{ include "ziti-controller.console" . }}
